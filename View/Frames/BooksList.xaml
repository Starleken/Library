<Page x:Class="SchoolLearn.View.Lists.BooksList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SchoolLearn.View.Lists"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1" Background="White">

    <Grid>
        <ListView Name="BookList">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Title}">Название</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Price}">Цена</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=PagesCount}">Количество страниц</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=beginreading}">Дата начала чтения</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=endreading}">Дата конца чтения</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
